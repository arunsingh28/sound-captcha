export declare class Tokenizer {
    regexFuncs: Array<() => RegExp>;
    flags: string;
    totalRegex: RegExp;
    constructor(regexFuncs: Array<() => RegExp>, flags?: string);
    run(text: string): string[];
    toString(): string;
    private combineRegex;
}
