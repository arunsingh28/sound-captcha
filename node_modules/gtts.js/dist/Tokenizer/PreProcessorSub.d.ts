import { PreProcessorRegex } from "./PreProcessorRegex";
export declare class PreProcessorSub {
    preProcessors: PreProcessorRegex[];
    constructor(subPairs: Array<Array<string | RegExp>>, ignoreCase?: boolean);
    run(text: string): string;
    toString(): string;
}
